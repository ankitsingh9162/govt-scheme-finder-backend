# MongoDB Connection (We'll add this later from MongoDB Atlas)
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret (Change this to any random string)
JWT_SECRET=your_secret_key_change_this_to_anything_random

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development
